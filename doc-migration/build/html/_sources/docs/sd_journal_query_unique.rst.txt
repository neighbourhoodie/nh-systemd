.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: sd_journal_query_unique
:manvolnum: 3
:summary: Read unique data fields from the journal
:aliases: sd_journal_enumerate_unique, sd_journal_enumerate_available_unique, sd_journal_restart_unique, SD_JOURNAL_FOREACH_UNIQUE

.. _sd_journal_query_unique(3):

==========================
sd_journal_query_unique(3)
==========================

Name
####

sd_journal_query_unique, sd_journal_enumerate_unique, sd_journal_enumerate_available_unique, sd_journal_restart_unique, SD_JOURNAL_FOREACH_UNIQUE — Read unique data fields from the journal

Synopsis
########

:code:`#include <systemd/sd-journal.h>` .. code-block::

   int sd_journal_query_unique(sd_journal *j,
	const char *field); .. code-block::

   int sd_journal_enumerate_available_unique(sd_journal *j,
	const void **data,
	size_t *length); .. code-block::

   int sd_journal_enumerate_unique(sd_journal *j,
	const void **data,
	size_t *length); .. code-block::

   void sd_journal_restart_unique(sd_journal *j); .. code-block::

   SD_JOURNAL_FOREACH_UNIQUE(sd_journal *j,
	const void *data,
	size_t length);

Description
###########

:code:`sd_journal_query_unique()` queries the journal for all unique values the specified field can take. It takes two arguments: the journal to query and the field name to look for. Well-known field names are listed on :ref:`systemd.journal-fields(7)`, but any field can be specified. Field names must be specified without a trailing "=". After this function has been executed successfully the field values may be queried using :code:`sd_journal_enumerate_unique()` and :code:`sd_journal_enumerate_available_unique()`. Invoking one of those calls will change the field name being queried and reset the enumeration index to the first field value that matches.

:code:`sd_journal_enumerate_unique()` may be used to iterate through all data fields which match the previously selected field name as set with :code:`sd_journal_query_unique()`. On each invocation the next field data matching the field name is returned. The order of the returned data fields is not defined. It takes three arguments: the journal object, plus a pair of pointers to pointer/size variables where the data object and its size shall be stored. The returned data is in a read-only memory map and is only valid until the next invocation of :code:`sd_journal_enumerate_unique()`. Note that the data returned will be prefixed with the field name and "=". Note that this call is subject to the data field size threshold as controlled by :code:`sd_journal_set_data_threshold()` and only the initial part of the field up to the threshold is returned. An error is returned for fields which cannot be retrieved. See the error list below for details.

:code:`sd_journal_enumerate_available_unique()` is similar to :code:`sd_journal_enumerate_unique()`, but silently skips any fields which may be valid, but are too large or not supported by current implementation.

:code:`sd_journal_restart_unique()` resets the data enumeration index to the beginning of the list. The next invocation of :code:`sd_journal_enumerate_unique()` will return the first field data matching the field name again.

Note that the :code:`SD_JOURNAL_FOREACH_UNIQUE()` macro may be used as a handy wrapper around :code:`sd_journal_restart_unique()` and :code:`sd_journal_enumerate_available_unique()`.

Note that these functions currently are not influenced by matches set with :code:`sd_journal_add_match()` but this might change in a later version of this software.

To enumerate all field names currently in use (and thus all suitable field parameters for :code:`sd_journal_query_unique()`), use the :ref:`sd_journal_enumerate_fields(3)` call.

Return Value
############

:code:`sd_journal_query_unique()` returns 0 on success or a negative errno-style error code. :code:`sd_journal_enumerate_unique()` and :code:`sd_journal_query_available_unique()` return a positive integer if the next field data has been read, 0 when no more fields remain, or a negative errno-style error code. :code:`sd_journal_restart_unique()` does not return anything.

Errors
######

Returned errors may indicate the following problems:

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove EINVAL|
    :end-before: .. inclusion-end-marker-do-not-remove EINVAL|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove ECHILD|
    :end-before: .. inclusion-end-marker-do-not-remove ECHILD|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove EADDRNOTAVAIL|
    :end-before: .. inclusion-end-marker-do-not-remove EADDRNOTAVAIL|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove ENOENT|
    :end-before: .. inclusion-end-marker-do-not-remove ENOENT|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove ENOBUFS|
    :end-before: .. inclusion-end-marker-do-not-remove ENOBUFS|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove E2BIG|
    :end-before: .. inclusion-end-marker-do-not-remove E2BIG|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove EPROTONOSUPPORT|
    :end-before: .. inclusion-end-marker-do-not-remove EPROTONOSUPPORT|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove EBADMSG|
    :end-before: .. inclusion-end-marker-do-not-remove EBADMSG|

.. include:: sd_journal_get_data.rst
    :start-after: .. inclusion-marker-do-not-remove EIO|
    :end-before: .. inclusion-end-marker-do-not-remove EIO|

Notes
#####

.. include:: threads-aware.rst
    :start-after: .. inclusion-marker-do-not-remove strict|
    :end-before: .. inclusion-end-marker-do-not-remove strict|

.. include:: libsystemd-pkgconfig.rst
    :start-after: .. inclusion-marker-do-not-remove pkgconfig-text|
    :end-before: .. inclusion-end-marker-do-not-remove pkgconfig-text|

Examples
########

Use the :code:`SD_JOURNAL_FOREACH_UNIQUE()` macro to iterate through all values a field of the journal can take (and which can be accessed on the given architecture and are not compressed with an unsupported mechanism). The following example lists all unit names referenced in the journal:

.. literalinclude:: /code-examples/c/journal-iterate-unique.c
    :language: c

History
#######

:code:`sd_journal_query_unique()`, :code:`sd_journal_enumerate_unique()`, :code:`sd_journal_restart_unique()`, and :code:`SD_JOURNAL_FOREACH_UNIQUE()` were added in version 195.

:code:`sd_journal_enumerate_available_unique()` was added in version 246.

See Also
########

:ref:`systemd(1)`, :ref:`systemd.journal-fields(7)`, :ref:`sd-journal(3)`, :ref:`sd_journal_open(3)`, :ref:`sd_journal_enumerate_fields(3)`, :ref:`sd_journal_get_data(3)`, :ref:`sd_journal_add_match(3)`


