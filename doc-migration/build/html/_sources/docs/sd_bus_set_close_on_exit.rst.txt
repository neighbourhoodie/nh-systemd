.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: sd_bus_set_close_on_exit
:manvolnum: 3
:summary: Control whether to close the bus connection during the event loop exit phase
:aliases: sd_bus_get_close_on_exit

.. _sd_bus_set_close_on_exit(3):

===========================
sd_bus_set_close_on_exit(3)
===========================

Name
####

sd_bus_set_close_on_exit, sd_bus_get_close_on_exit — Control whether to close the bus connection during the event loop exit phase

Synopsis
########

:code:`#include <systemd/sd-bus.h>` .. code-block::

   int sd_bus_set_close_on_exit(sd_bus *bus,
	int b); .. code-block::

   int sd_bus_get_close_on_exit(sd_bus *bus);

Description
###########

:code:`sd_bus_set_close_on_exit()` may be used to enable or disable whether the bus connection is automatically flushed (as in :ref:`sd_bus_flush(3)`) and closed (as in :ref:`sd_bus_close(3)`) during the exit phase of the event loop. This logic only applies to bus connections that are attached to an :ref:`sd-event(3)` event loop, see :ref:`sd_bus_attach_event(3)`. By default, this mechanism is enabled and makes sure that any pending messages that have not been written to the bus connection are written out when the event loop is shutting down. In some cases this behaviour is not desirable, for example when the bus connection shall remain usable until after the event loop exited. If :code:`b` is true, the feature is enabled (which is the default), otherwise disabled.

:code:`sd_bus_get_close_on_exit()` may be used to query the current setting of this feature. It returns zero when the feature is disabled, and positive if enabled.

Return Value
############

On success, :code:`sd_bus_set_close_on_exit()` returns a non-negative integer. On failure, it returns a negative errno-style error code.

:code:`sd_bus_get_close_on_exit()` returns 0 if the feature is currently disabled or a positive integer if it is enabled. On failure, it returns a negative errno-style error code.

Errors
######

Returned errors may indicate the following problems:

.. systemd:option:: -ECHILD

   The bus connection was created in a different process, library or module instance.

.. include:: libsystemd-pkgconfig.rst

History
#######

:code:`sd_bus_set_close_on_exit()` and :code:`sd_bus_get_close_on_exit()` were added in version 240.

See Also
########

:ref:`systemd(1)`, :ref:`sd-bus(3)`, :ref:`sd_bus_flush(3)`, :ref:`sd_bus_attach_event(3)`, :ref:`sd-event(3)`, :ref:`sd_event_add_exit(3)`


