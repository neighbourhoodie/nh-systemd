.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: systemd.generator
:manvolnum: 7
:summary: systemd unit generators

.. _systemd.generator(7):

====================
systemd.generator(7)
====================

Name
####

systemd.generator — systemd unit generators

Synopsis
########

:code:`/path/to/generator normal-dir [<early-dir>] [<late-dir>]`

| :code:`/run/systemd/system-generators/*`
| :code:`/etc/systemd/system-generators/*`
| :code:`/usr/local/lib/systemd/system-generators/*`
| :code:`|SYSTEM_GENERATOR_DIR|/*`

| :code:`/run/systemd/user-generators/*`
| :code:`/etc/systemd/user-generators/*`
| :code:`/usr/local/lib/systemd/user-generators/*`
| :code:`|USER_GENERATOR_DIR|/*`

Description
###########

Generators are small executables placed in :code:`|SYSTEM_GENERATOR_DIR|/` and other directories listed above. :ref:`systemd(1)` will execute these binaries very early at bootup and at configuration reload time — before unit files are loaded. Their main purpose is to convert configuration and execution context parameters that are not native to the service manager into dynamically generated unit files, symlinks or unit file drop-ins, so that they can extend the unit file hierarchy the service manager subsequently loads and operates on.

:code:`systemd` will call each generator with three directory paths that are to be used for generator output. In these three directories, generators may dynamically generate unit files (regular ones, instances, as well as templates), unit file :code:`.d/` drop-ins, and create symbolic links to unit files to add additional dependencies, create aliases, or instantiate existing templates. Those directories are included in the unit load path, allowing generated configuration to extend or override existing definitions. For tests, generators may be called with just one argument; the generator should assume that all three paths are the same in that case.

Generators executed by the system manager are invoked in a sandbox with a private writable :code:`/tmp/` directory and where most of the file system is read-only except for the generator output directories.

Directory paths for generator output differ by priority: :code:`…/generator.early` has priority higher than the admin configuration in :code:`/etc/`, while :code:`…/generator` has lower priority than :code:`/etc/` but higher than vendor configuration in :code:`/usr/`, and :code:`…/generator.late` has priority lower than all other configuration. See the next section and the discussion of unit load paths and unit overriding in :ref:`systemd.unit(5)`.

Generators are loaded from a set of paths determined during compilation, as listed above. System and user generators are loaded from directories with names ending in :code:`system-generators/` and :code:`user-generators/`, respectively. Generators found in directories listed earlier override the ones with the same name in directories lower in the list [#usrlocalfootnote]_. A symlink to :code:`/dev/null` or an empty file can be used to mask a generator, thereby preventing it from running. Please note that the order of the two directories with the highest priority is reversed with respect to the unit load path, and generators in :code:`/run/` overwrite those in :code:`/etc/`.

After installing new generators or updating the configuration, :code:`systemctl daemon-reload` may be executed. This will delete the previous configuration created by generators, re-run all generators, and cause :code:`systemd` to reload units from disk. See :man-pages:`systemctl(1)` for more information.

Output directories
##################

Generators are invoked with three arguments: paths to directories where generators can place their generated unit files or symlinks. By default, those paths are runtime directories that are included in the search path of :code:`systemd`, but a generator may be called with different paths for debugging purposes. If only one argument is provided, the generator should use the same directory as the three output paths.

1.    :code:`normal-dir`

   In normal use this is :code:`/run/systemd/generator` in case of the system generators and :code:`$XDG_RUNTIME_DIR/systemd/generator` in case of the user generators. Unit files placed in this directory take precedence over vendor unit configuration but not over native user/administrator unit configuration.

2.    :code:`early-dir`

   In normal use this is :code:`/run/systemd/generator.early` in case of the system generators and :code:`$XDG_RUNTIME_DIR/systemd/generator.early` in case of the user generators. Unit files placed in this directory override unit files in :code:`/usr/`, :code:`/run/` and :code:`/etc/`. This means that unit files placed in this directory take precedence over all normal configuration, both vendor and user/administrator.

3.    :code:`late-dir`

   In normal use this is :code:`/run/systemd/generator.late` in case of the system generators and :code:`$XDG_RUNTIME_DIR/systemd/generator.late` in case of the user generators. This directory may be used to extend the unit file tree without overriding any other unit files. Any native configuration files supplied by the vendor or user/administrator take precedence.

Note: generators *must not* write to other locations or otherwise make changes to system state. Generator output is supposed to last only until the next write to other locations or otherwise make changes to system state. Generator output is supposed to last only until the next :code:`daemon-reload` or :code:`daemon-reexec`; if the generator is replaced or masked, its effects should vanish.

Environment
###########

The service manager sets a number of environment variables when invoking generator executables. They carry information about the execution context of the generator, in order to simplify conditionalizing generators to specific environments. The following environment variables are set:

.. systemd:var:: $SYSTEMD_SCOPE
   :group: environment-variables

   If the generator is invoked from the system service manager this variable is set to "system"; if invoked from the per-user service manager it is set to "user".

   .. versionadded:: 251

.. systemd:var:: $SYSTEMD_IN_INITRD
   :group: environment-variables

   If the generator is run as part of an initrd this is set to "1". If it is run from the regular host (i.e. after the transition from initrd to host) it is set to "0". This environment variable is only set for system generators.

   .. versionadded:: 251

.. systemd:var:: $SYSTEMD_SOFT_REBOOTS_COUNT
   :group: environment-variables

   If the system has soft-rebooted, this variable is set to the count of soft-reboots. This environment variable is only set for system generators.

   .. versionadded:: 257

.. systemd:var:: $SYSTEMD_FIRST_BOOT
   :group: environment-variables

   If this boot-up cycle is considered a "first boot", this is set to "1"; if it is a subsequent, regular boot it is set to "0". For details see the documentation of :systemd:var:`ConditionFirstBoot=` in :ref:`systemd.unit(5)`. This environment variable is only set for system generators.

   .. versionadded:: 251

.. systemd:var:: $SYSTEMD_VIRTUALIZATION
   :group: environment-variables

   If the service manager is run in a virtualized environment, :systemd:var:`$SYSTEMD_VIRTUALIZATION` is set to a pair of strings, separated by a colon. The first string is either "vm" or "container", categorizing the type of virtualization. The second string identifies the implementation of the virtualization technology. If no virtualization is detected this variable will not be set. This data is identical to what :ref:`systemd-detect-virt(1)` detects and reports, and uses the same vocabulary of virtualization implementation identifiers.

   .. versionadded:: 251

.. systemd:var:: $SYSTEMD_ARCHITECTURE
   :group: environment-variables

   This variable is set to a short identifier of the reported architecture of the system. For details about defined values, see documentation of :systemd:var:`ConditionArchitecture=` in :ref:`systemd.unit(5)`.

   .. versionadded:: 251

.. systemd:var:: $CREDENTIALS_DIRECTORY, $ENCRYPTED_CREDENTIALS_DIRECTORY
   :group: environment-variables

   If set, refers to the directory system credentials have been placed in. Credentials passed into the system in plaintext form will be placed in :systemd:var:`$CREDENTIALS_DIRECTORY`, and those passed in in encrypted form will be placed in :systemd:var:`$ENCRYPTED_CREDENTIALS_DIRECTORY`. Use the :ref:`systemd-creds(1)` command to automatically decrypt/authenticate credentials passed in, if needed. Specifically, use the :code:`systemd-creds --system cat` command.

   .. versionadded:: 254

.. systemd:var:: $SYSTEMD_CONFIDENTIAL_VIRTUALIZATION
   :group: environment-variables

   If the service manager is run in a confidential virtualized environment, :systemd:var:`$SYSTEMD_CONFIDENTIAL_VIRTUALIZATION` is set to a string that identifies the confidential virtualization hardware technology. If no confidential virtualization is detected this variable will not be set. This data is identical to what :ref:`systemd-detect-virt(1)` detects and reports, and uses the same vocabulary of confidential virtualization technology identifiers.

   .. versionadded:: 254

Notes about writing generators
##############################

*    All generators are executed in parallel. That means all executables are started at the very same time and need to be able to cope with this parallelism.

*    Generators are run very early at boot and cannot rely on any external services. They may not talk to any other process. That includes simple things such as logging to :man-pages:`syslog(3)`, or :code:`systemd` itself (this means: no :man-pages:`systemctl(1)`)! Non-essential file systems like :code:`/var/` and :code:`/home/` are mounted after generators have run. Generators can however rely on the most basic kernel functionality to be available, as well as mounted :code:`/sys/`, :code:`/proc/`, :code:`/dev/`, :code:`/usr/` and :code:`/run/` file systems.

*    Units written by generators are removed when the configuration is reloaded. That means the lifetime of the generated units is closely bound to the reload cycles of :code:`systemd` itself.

*    Generators should only be used to generate unit files, :code:`.d/\*.conf` drop-ins for them and symlinks to them, not any other kind of non-unit related configuration. Due to the lifecycle logic mentioned above, generators are not a good fit to generate dynamic configuration for other services. If you need to generate dynamic configuration for other services, do so in normal services you order before the service in question.

   Note that using the :code:`StandardInputData=`/:code:`StandardInputText=` settings of service unit files (see :ref:`systemd.exec(5)`), it is possible to make arbitrary input data (including daemon-specific configuration) part of the unit definitions, which often might be sufficient to embed data or configuration for other programs into unit files in a native fashion.

*    Since :man-pages:`syslog(3)` is not available (see above), log messages have to be written to :code:`/dev/kmsg` instead.

*    The generator should always include its own name in a comment at the top of the generated file, so that the user can easily figure out which component created or amended a particular unit.

   The :code:`SourcePath=` directive should be used in generated files to specify the source configuration file they are generated from. This makes things more easily understood by the user and also has the benefit that systemd can warn the user about configuration files that changed on disk but have not been read yet by systemd. The :code:`SourcePath=` value does not have to be a file in a physical filesystem. For example, in the common case of the generator looking at the kernel command line, :code:`SourcePath=/proc/cmdline` should be used.

*    Generators may write out dynamic unit files or just hook unit files into other units with the usual :code:`.wants/` or :code:`.requires/` symlinks. Often, it is nicer to simply instantiate a template unit file from :code:`/usr/` with a generator instead of writing out entirely dynamic unit files. Of course, this works only if a single parameter is to be used.

*    If you are careful, you can implement generators in shell scripts. We do recommend C code however, since generators are executed synchronously and hence delay the entire boot if they are slow.

*    Regarding overriding semantics: there are two rules we try to follow when thinking about the overriding semantics:

   1.       User configuration should override vendor configuration. This (mostly) means that stuff from :code:`/etc/` should override stuff from :code:`/usr/`.

   2.       Native configuration should override non-native configuration. This (mostly) means that stuff you generate should never override native unit files for the same purpose.

   Of these two rules the first rule is probably the more important one and breaks the second one sometimes. Hence, when deciding whether to use argv[1], argv[2], or argv[3], your default choice should probably be argv[1].

*    Instead of heading off now and writing all kind of generators for legacy configuration file formats, please think twice! It is often a better idea to just deprecate old stuff instead of keeping it artificially alive.

Examples
########

systemd-fstab-generator
-----------------------

:ref:`systemd-fstab-generator(8)` converts :code:`/etc/fstab` into native mount units. It uses argv[1] as location to place the generated unit files in order to allow the user to override :code:`/etc/fstab` with their own native unit files, but also to ensure that :code:`/etc/fstab` overrides any vendor default from :code:`/usr/`.

After editing :code:`/etc/fstab`, the user should invoke :code:`systemctl daemon-reload`. This will re-run all generators and cause :code:`systemd` to reload units from disk. To actually mount new directories added to :code:`fstab`, :code:`systemctl start /path/to/mountpoint` or :code:`systemctl start local-fs.target` may be used.

systemd-system-update-generator
-------------------------------

:ref:`systemd-system-update-generator(8)` temporarily redirects :code:`default.target` to :code:`system-update.target`, if a system update is scheduled. Since this needs to override the default user configuration for :code:`default.target`, it uses argv[2]. For details about this logic, see :ref:`systemd.offline-updates(7)`.

Debugging a generator
---------------------

.. code-block:: sh

   dir=$(mktemp -d)
   SYSTEMD_LOG_LEVEL=debug |SYSTEM_GENERATOR_DIR|/systemd-fstab-generator \
           "$dir" "$dir" "$dir"
   find $dir

See Also
########

:ref:`systemd(1)`, :ref:`systemd-cryptsetup-generator(8)`, :ref:`systemd-debug-generator(8)`, :ref:`systemd-fstab-generator(8)`, :man-pages:`fstab(5)`, :ref:`systemd-getty-generator(8)`, :ref:`systemd-gpt-auto-generator(8)`, :ref:`systemd-hibernate-resume-generator(8)`, :ref:`systemd-rc-local-generator(8)`, :ref:`systemd-system-update-generator(8)`, :ref:`systemd-sysv-generator(8)`, :ref:`systemd-xdg-autostart-generator(8)`, :ref:`systemd.unit(5)`, :man-pages:`systemctl(1)`, :ref:`systemd.environment-generator(7)`



.. rubric:: Footnotes

%% include="standard-conf.rst" id="usr-local-footnote|" %%
