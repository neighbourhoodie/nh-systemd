.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: systemd-fsck@.service
:manvolnum: 8
:summary: File system checker logic
:aliases: systemd-fsck-root.service, systemd-fsck-usr.service, systemd-fsck

.. _systemd-fsck@.service(8):

========================
systemd-fsck@.service(8)
========================

Name
####

systemd-fsck@.service, systemd-fsck-root.service, systemd-fsck-usr.service, systemd-fsck — File system checker logic

Synopsis
########

| :code:`systemd-fsck@.service`
| :code:`systemd-fsck-root.service`
| :code:`systemd-fsck-usr.service`
| :code:`/usr/lib/systemd/systemd-fsck`

Description
###########

:code:`systemd-fsck@.service`, :code:`systemd-fsck-root.service`, and :code:`systemd-fsck-usr.service` are services responsible for file system checks. They are instantiated for each device that is configured for file system checking. :code:`systemd-fsck-root.service` and :code:`systemd-fsck-usr.service` are responsible for file system checks on the root and /usr file system, respectively, but only if the root filesystem was not checked in the initrd. :code:`systemd-fsck@.service` is used for all other file systems and for the root file system in the initrd.

These services are started at boot if :code:`passno` in :code:`/etc/fstab` for the file system is set to a value greater than zero, but only if it is also configured to be mounted at boot (i.e. without "noauto" option). The file system check for root is performed before the other file systems. Other file systems may be checked in parallel, except when they are on the same rotating disk.

:code:`systemd-fsck` does not know any details about specific filesystems, and simply executes file system checkers specific to each filesystem type (:code:`fsck.type`). These checkers will decide if the filesystem should actually be checked based on the time since last check, number of mounts, unclean unmount, etc.

:code:`systemd-fsck-root.service` and :code:`systemd-fsck-usr.service` will activate :code:`reboot.target` if :man-pages:`fsck(8)` returns the "System should reboot" condition, or :code:`emergency.target` if :code:`fsck` returns the "Filesystem errors left uncorrected" condition.

:code:`systemd-fsck@.service` will fail if :man-pages:`fsck(8)` returns either the "System should reboot" or the "Filesystem errors left uncorrected" condition. For filesystems listed in :code:`/etc/fstab` without "nofail" or "noauto" options, :code:`local-fs.target` will then activate :code:`emergency.target`.

Kernel Command Line
###################

:code:`systemd-fsck` understands these kernel command line parameters:

.. systemd:option:: fsck.mode=
   :group: kernel-commandline-options

   One of "auto", "force", "skip". Controls the mode of operation. The default is "auto", and ensures that file system checks are done when the file system checker deems them necessary. "force" unconditionally results in full file system checks. "skip" skips any file system checks.

   .. versionadded:: 186

.. systemd:option:: fsck.repair=
   :group: kernel-commandline-options

   One of "preen", "yes", "no". Controls the mode of operation. The default is "preen", and will automatically repair problems that can be safely fixed. "yes" will answer yes to all questions by fsck and "no" will answer no to all questions.

   .. versionadded:: 213

Credentials
###########

:code:`systemd-fsck` supports the service credentials logic as implemented by :code:`ImportCredential=`/:code:`LoadCredential=`/:code:`SetCredential=` (see :ref:`systemd.exec(5)` for details). The following credentials are used when passed in:

.. systemd:option:: fsck.mode, fsck.repair
   :group: system-credentials

   The contents of the credentials are parsed as same as the kernel command line options with the same name. See above for more details.

   .. versionadded:: 258

Note that by default the :code:`systemd-fsck@.service`, :code:`systemd-fsck-root.service`, and :code:`systemd-fsck-usr.service` unit files are set up to inherit both :code:`fsck.mode` and :code:`fsck.repair` credentials from the service manager.

See Also
########

:ref:`systemd(1)`, :man-pages:`fsck(8)`, :ref:`systemd-quotacheck@.service(8)`, `fsck.btrfs(8) <https://btrfs.readthedocs.io/en/latest/fsck.btrfs.html>`_, :man-pages:`fsck.cramfs(8)`, :man-pages:`fsck.ext4(8)`, :man-pages:`fsck.fat(8)`, :man-pages:`fsck.hfsplus(8)`, :man-pages:`fsck.minix(8)`, :man-pages:`fsck.ntfs(8)`, :man-pages:`fsck.xfs(8)`


