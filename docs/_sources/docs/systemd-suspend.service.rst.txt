.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: systemd-suspend.service
:manvolnum: 8
:summary: System sleep state logic
:aliases: systemd-hibernate.service, systemd-hybrid-sleep.service, systemd-suspend-then-hibernate.service, systemd-sleep

.. _systemd-suspend.service(8):

==========================
systemd-suspend.service(8)
==========================

Name
####

systemd-suspend.service, systemd-hibernate.service, systemd-hybrid-sleep.service, systemd-suspend-then-hibernate.service, systemd-sleep — System sleep state logic

Synopsis
########

| :code:`systemd-suspend.service`
| :code:`systemd-hibernate.service`
| :code:`systemd-hybrid-sleep.service`
| :code:`systemd-suspend-then-hibernate.service`
| :code:`/usr/lib/systemd/system-sleep`

Description
###########

:code:`systemd-suspend.service` is a system service that is pulled in by :code:`suspend.target` and is responsible for the actual system suspend. Similarly, :code:`systemd-hibernate.service` is pulled in by :code:`hibernate.target` to execute the actual hibernation. Finally, :code:`systemd-hybrid-sleep.service` is pulled in by :code:`hybrid-sleep.target` to execute hybrid hibernation with system suspend and pulled in by :code:`suspend-then-hibernate.target` to execute system suspend with a timeout that will activate hibernate later.

Immediately before entering system suspend and/or hibernation :code:`systemd-suspend.service` (and the other mentioned units, respectively) will run all executables in :code:`/usr/lib/systemd/system-sleep/` and pass two arguments to them. The first argument will be "pre", the second either "suspend", "hibernate", "hybrid-sleep", or "suspend-then-hibernate" depending on the chosen action. An environment variable called "SYSTEMD_SLEEP_ACTION" will be set and contain the sleep action that is processing. This is primarily helpful for "suspend-then-hibernate" where the value of the variable will be "suspend", "hibernate", or "suspend-after-failed-hibernate" in cases where hibernation has failed. Immediately after leaving system suspend and/or hibernation the same executables are run, but the first argument is now "post". All executables in this directory are executed in parallel, and execution of the action is not continued until all executables have finished. Note that :code:`user.slice` will be frozen while the executables are running, so they should not attempt to communicate with any user services expecting a reply.

Note that scripts or binaries dropped in :code:`/usr/lib/systemd/system-sleep/` are intended for local use only and should be considered hacks. If applications want to react to system suspend/hibernation and resume, they should rather use the `Inhibitor Locks <https://systemd.io/INHIBITOR_LOCKS>`_.

Note that :code:`systemd-suspend.service`, :code:`systemd-hibernate.service`, :code:`systemd-hybrid-sleep.service`, and :code:`systemd-suspend-then-hibernate.service` should never be executed directly. Instead, trigger system sleep with a command such as :code:`systemctl suspend` or :code:`systemctl hibernate`.

Internally, this service will echo a string like "mem" into :code:`/sys/power/state`, to trigger the actual system suspend. What exactly is written where can be configured in the \[Sleep] section of :code:`/etc/systemd/sleep.conf` or a :code:`sleep.conf.d` file. See :ref:`systemd-sleep.conf(5)`.

Note that by default these services freeze :code:`user.slice` while they run. This prevents the execution of any process in any of the user sessions while the system is entering into and resuming from sleep. Thus, this prevents the hooks in :code:`/usr/lib/systemd/system-sleep/`, or any other process for that matter, from communicating with any user session process during sleep.

Options
#######

:code:`systemd-sleep` understands the following commands:

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove help|
    :end-before: .. inclusion-end-marker-do-not-remove help|

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove version|
    :end-before: .. inclusion-end-marker-do-not-remove version|

.. systemd:option:: suspend, hibernate, suspend-then-hibernate, hybrid-sleep

   Suspend, hibernate, suspend then hibernate, or put the system to hybrid sleep.

   .. versionadded:: 203

See Also
########

:ref:`systemd-sleep.conf(5)`, :ref:`systemd(1)`, :man-pages:`systemctl(1)`, :ref:`systemd.special(7)`, :ref:`systemd-halt.service(8)`


