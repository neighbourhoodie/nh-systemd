.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: udev_device_get_syspath
:manvolnum: 3
:summary: Query device properties
:aliases: udev_device_get_sysname, udev_device_get_sysnum, udev_device_get_devpath, udev_device_get_devnode, udev_device_get_devnum, udev_device_get_devtype, udev_device_get_subsystem, udev_device_get_driver, udev_device_get_udev, udev_device_get_parent, udev_device_get_parent_with_subsystem_devtype, udev_device_get_is_initialized, udev_device_get_action

.. _udev_device_get_syspath(3):

==========================
udev_device_get_syspath(3)
==========================

Name
####

udev_device_get_syspath, udev_device_get_sysname, udev_device_get_sysnum, udev_device_get_devpath, udev_device_get_devnode, udev_device_get_devnum, udev_device_get_devtype, udev_device_get_subsystem, udev_device_get_driver, udev_device_get_udev, udev_device_get_parent, udev_device_get_parent_with_subsystem_devtype, udev_device_get_is_initialized, udev_device_get_action — Query device properties

Synopsis
########

:code:`#include <libudev.h>` .. code-block::

   const char *udev_device_get_syspath(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_sysname(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_sysnum(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_devpath(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_devnode(struct udev_device *udev_device); .. code-block::

   dev_t udev_device_get_devnum(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_devtype(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_subsystem(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_driver(struct udev_device *udev_device); .. code-block::

   struct udev *udev_device_get_udev(struct udev_device *udev_device); .. code-block::

   struct udev_device *udev_device_get_parent(struct udev_device *udev_device); .. code-block::

   struct udev_device *udev_device_get_parent_with_subsystem_devtype(struct udev_device *udev_device,
	const char *subsystem,
	const char *devtype); .. code-block::

   int udev_device_get_is_initialized(struct udev_device *udev_device); .. code-block::

   const char *udev_device_get_action(struct udev_device *udev_device);

.. COMMENT: <refsect1> <title>Description</title> <para>XXX: Add documentation.</para> </refsect1>

Return Value
############

On success, :code:`udev_device_get_syspath()`, :code:`udev_device_get_sysname()`, :code:`udev_device_get_sysnum()`, :code:`udev_device_get_devpath()`, :code:`udev_device_get_devnode()`, :code:`udev_device_get_devtype()`, :code:`udev_device_get_subsystem()`, :code:`udev_device_get_driver()` and :code:`udev_device_get_action()` return a pointer to a constant string that describes the requested property. The lifetime of this string is bound to the device it was requested on. On failure, each function may return :code:`NULL`.

On success, :code:`udev_device_get_devnum()` returns the device type of the passed device. On failure, a device type with minor and major number set to :code:`0` is returned.

:code:`udev_device_get_udev()` always returns a valid pointer to the udev context that this device belongs to.

On success, :code:`udev_device_get_parent()` and :code:`udev_device_get_parent_with_subsystem_devtype()` return a pointer to the parent device. No additional reference to this device is acquired, but the child device owns a reference to such a parent device. On failure, :code:`NULL` is returned.

On success, :code:`udev_device_get_is_initialized()` returns either :code:`1` or :code:`0`, depending on whether the passed device has already been initialized by udev or not. On failure, a negative error code is returned. Note that devices for which no udev rules are defined are never reported initialized.

History
#######

:code:`udev_device_get_syspath()`, :code:`udev_device_get_sysname()`, :code:`udev_device_get_sysnum()`, :code:`udev_device_get_devpath()`, :code:`udev_device_get_devnode()`, :code:`udev_device_get_devnum()`, :code:`udev_device_get_devtype()`, :code:`udev_device_get_subsystem()`, :code:`udev_device_get_driver()`, :code:`udev_device_get_udev()`, :code:`udev_device_get_parent()`, :code:`udev_device_get_parent_with_subsystem_devtype()`, :code:`udev_device_get_is_initialized()`, and :code:`udev_device_get_action()` were added in version 221.

See Also
########

:man-pages:`udev_new(3)`, :man-pages:`udev_device_new_from_syspath(3)`, :man-pages:`udev_device_has_tag(3)`, :man-pages:`udev_enumerate_new(3)`, :man-pages:`udev_monitor_new_from_netlink(3)`, :man-pages:`udev_list_entry(3)`, :ref:`systemd(1)`


