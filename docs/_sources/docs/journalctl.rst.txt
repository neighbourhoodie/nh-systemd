.. SPDX-License-Identifier: LGPL-2.1-or-later

:title: journalctl
:manvolnum: 1
:summary: Print log entries from the systemd journal

.. _journalctl(1):

=============
journalctl(1)
=============

Name
####

journalctl — Print log entries from the systemd journal

Synopsis
########

:code:`journalctl [OPTIONS...] [MATCHES...]`

Description
###########

:code:`journalctl` is used to print the log entries stored in the journal by :ref:`systemd-journald.service(8)` and :ref:`systemd-journal-remote.service(8)`.

If called without parameters, it will show the contents of the journal accessible to the calling user, starting with the oldest entry collected.

If one or more match arguments are passed, the output is filtered accordingly. A match is in the format "FIELD=VALUE", e.g. "_SYSTEMD_UNIT=httpd.service", referring to the components of a structured journal entry. See :ref:`systemd.journal-fields(7)` for a list of well-known fields. If multiple matches are specified matching different fields, the log entries are filtered by both, i.e. the resulting output will show only entries matching all the specified matches of this kind. If two matches apply to the same field, then they are automatically matched as alternatives, i.e. the resulting output will show entries matching any of the specified matches for the same field. Finally, the character "+" may appear as a separate word between other terms on the command line. This causes all matches before and after to be combined in a disjunction (i.e. logical OR).

It is also possible to filter the entries by specifying an absolute file path as an argument. The file path may be a file or a symbolic link and the file must exist at the time of the query. If a file path refers to an executable binary, an "_EXE=" match for the canonicalized binary path is added to the query. If a file path refers to an executable script, a "_COMM=" match for the script name is added to the query. If a file path refers to a device node, "_KERNEL_DEVICE=" matches for the kernel name of the device and for each of its ancestor devices is added to the query. Symbolic links are dereferenced, kernel names are synthesized, and parent devices are identified from the environment at the time of the query. In general, a device node is the best proxy for an actual device, as log entries do not usually contain fields that identify an actual device. For the resulting log entries to be correct for the actual device, the relevant parts of the environment at the time the entry was logged, in particular the actual device corresponding to the device node, must have been the same as those at the time of the query. Because device nodes generally change their corresponding devices across reboots, specifying a device node path causes the resulting entries to be restricted to those from the current boot.

Additional constraints may be added using options :code:`--boot`, :code:`--unit=`, etc., to further limit what entries will be shown (logical AND).

Output is interleaved from all accessible journal files, whether they are rotated or currently being written, and regardless of whether they belong to the system itself or are accessible user journals. The :code:`--header` option can be used to identify which files *are* being shown. being shown.

The set of journal files which will be used can be modified using the :code:`--user`, :code:`--system`, :code:`--directory=`, and :code:`--file=` options, see below.

All users are granted access to their private per-user journals. However, by default, only root and users who are members of a few special groups are granted access to the system journal and the journals of other users. Members of the groups "systemd-journal", "adm", and "wheel" can read all journal files. Note that the two latter groups traditionally have additional privileges specified by the distribution. Members of the "wheel" group can often perform administrative tasks.

The output is paged through :code:`less` by default, and long lines are "truncated" to screen width. The hidden part can be viewed by using the left-arrow and right-arrow keys. Paging can be disabled; see the :code:`--no-pager` option and the "Environment" section below.

When outputting to a tty, lines are colored according to priority: lines of level ERROR and higher are colored red; lines of level WARNING are colored yellow; lines of level NOTICE are highlighted; lines of level INFO are displayed normally; lines of level DEBUG are colored grey.

To write entries *to* the journal, a few methods may be used. In general, output from systemd units is automatically connected to the journal, see the journal, a few methods may be used. In general, output from systemd units is automatically connected to the journal, see :ref:`systemd-journald.service(8)`. In addition, :ref:`systemd-cat(1)` may be used to send messages to the journal directly.

Source Options
##############

The following options control where to read journal records from:

.. systemd:option:: --system, --user

   Show messages from system services and the kernel (with :code:`--system`). Show messages from service of current user (with :code:`--user`). If neither is specified, show all messages that the user can see.

   The :code:`--user` option affects how :code:`--unit=` arguments are treated. See :code:`--unit=`.

   Note that :code:`--user` only works if persistent logging is enabled, via the :code:`Storage=` setting in :man-pages:`journald.conf(5)`.

   .. versionadded:: 205

.. systemd:option:: -M, --machine=

   Show messages from a running, local container. Specify a container name to connect to.

   .. versionadded:: 209

.. systemd:option:: -m, --merge

   Show entries interleaved from all available journals, including remote ones.

   .. versionadded:: 190

.. systemd:option:: -D DIR, --directory=DIR

   Takes a directory path as argument. If specified, journalctl will operate on the specified journal directory :emphasis:`DIR` instead of the default runtime and system journal paths.

   .. versionadded:: 187

.. systemd:option:: -i GLOB, --file=GLOB

   Takes a file glob as an argument. If specified, journalctl will operate on the specified journal files matching :emphasis:`GLOB` instead of the default runtime and system journal paths. May be specified multiple times, in which case files will be suitably interleaved.

   .. versionadded:: 205

.. systemd:option:: --root=ROOT

   Takes a directory path as an argument. If specified, :code:`journalctl` will operate on journal directories and catalog file hierarchy underneath the specified directory instead of the root directory (e.g. :code:`--update-catalog` will create :code:`ROOT/var/lib/systemd/catalog/database`, and journal files under :code:`ROOT/run/journal/` or :code:`ROOT/var/log/journal/` will be displayed).

   .. versionadded:: 201

.. systemd:option:: --image=IMAGE

   Takes a path to a disk image file or block device node. If specified, :code:`journalctl` will operate on the file system in the indicated disk image. This option is similar to :code:`--root=`, but operates on file systems stored in disk images or block devices, thus providing an easy way to extract log data from disk images. The disk image should either contain just a file system or a set of file systems within a GPT partition table, following the `Discoverable Partitions Specification <https://uapi-group.org/specifications/specs/discoverable_partitions_specification>`_. For further information on supported disk images, see :ref:`systemd-nspawn(1)`'s switch of the same name.

   .. versionadded:: 247

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove image-policy-open|
    :end-before: .. inclusion-end-marker-do-not-remove image-policy-open|

.. systemd:option:: --namespace=NAMESPACE

   Takes a journal namespace identifier string as argument. If not specified, the data collected by the default namespace is shown. If specified, shows the log data of the specified namespace instead. If the namespace is specified as "*" data from all namespaces is shown, interleaved. If the namespace identifier is prefixed with "+" data from the specified namespace and the default namespace is shown, interleaved, but no other. For details about journal namespaces see :ref:`systemd-journald.service(8)`.

   .. versionadded:: 245

Filtering Options
#################

The following options control how to filter journal records:

.. systemd:option:: -S, --since=, -U, --until=

   Start showing entries on or newer than the specified date, or on or older than the specified date, respectively. Date specifications should be of the format "2012-10-30 18:17:16". If the time part is omitted, "00:00:00" is assumed. If only the seconds component is omitted, ":00" is assumed. If the date component is omitted, the current day is assumed. Alternatively the strings "yesterday", "today", "tomorrow" are understood, which refer to 00:00:00 of the day before the current day, the current day, or the day after the current day, respectively. "now" refers to the current time. Finally, relative times may be specified, prefixed with "-" or "+", referring to times before or after the current time, respectively. For complete time and date specification, see :ref:`systemd.time(7)`. Note that :code:`--output=short-full` prints timestamps that follow precisely this format.

   .. versionadded:: 195

.. systemd:option:: -c, --cursor=

   Start showing entries from the location in the journal specified by the passed cursor.

   .. versionadded:: 193

.. systemd:option:: --after-cursor=

   Start showing entries from the location in the journal *after* the location specified by the passed cursor. The cursor is shown when the the location specified by the passed cursor. The cursor is shown when the :code:`--show-cursor` option is used.

   .. versionadded:: 206

.. systemd:option:: --cursor-file=FILE

   If :emphasis:`FILE` exists and contains a cursor, start showing entries *after* this location. Otherwise, show entries according to the other given options. At the end, write the cursor of the last entry to this location. Otherwise, show entries according to the other given options. At the end, write the cursor of the last entry to :emphasis:`FILE`. Use this option to continually read the journal by sequentially calling :code:`journalctl`.

   .. versionadded:: 242

.. systemd:option:: -b [[ID][±offset]|all], --boot[=[ID][±offset]|all]

   Show messages from a specific boot. This will add a match for "_BOOT_ID=".

   The argument may be empty, in which case logs for the current boot will be shown.

   If the boot ID is omitted, a positive :emphasis:`offset` will look up the boots starting from the beginning of the journal, and an equal-or-less-than zero :emphasis:`offset` will look up boots starting from the end of the journal. Thus, :code:`1` means the first boot found in the journal in chronological order, :code:`2` the second and so on; while :code:`-0` is the last boot, :code:`-1` the boot before last, and so on. An empty :emphasis:`offset` is equivalent to specifying :code:`-0`, except when the current boot is not the last boot (e.g. because :code:`--directory=` was specified to look at logs from a different machine).

   If the 32-character :emphasis:`ID` is specified, it may optionally be followed by :emphasis:`offset` which identifies the boot relative to the one given by boot :emphasis:`ID`. Negative values mean earlier boots and positive values mean later boots. If :emphasis:`offset` is not specified, a value of zero is assumed, and the logs for the boot given by :emphasis:`ID` are shown.

   The special argument :code:`all` can be used to negate the effect of an earlier use of :code:`-b`.

   .. versionadded:: 186

.. systemd:option:: -u, --unit=UNIT|PATTERN

   Show messages for the specified systemd unit :emphasis:`UNIT` (such as a service unit), or for any of the units matched by :emphasis:`PATTERN`. If a pattern is specified, a list of unit names found in the journal is compared with the specified pattern and all that match are used. For each unit name, a match is added for messages from the unit ("_SYSTEMD_UNIT=:emphasis:`UNIT`"), along with additional matches for messages from systemd and messages about coredumps for the specified unit. A match is also added for "_SYSTEMD_SLICE=:emphasis:`UNIT`", such that if the provided :emphasis:`UNIT` is a :ref:`systemd.slice(5)` unit, all logs of children of the slice will be shown.

   With :code:`--user`, all :code:`--unit=` arguments will be converted to match user messages as if specified with :code:`--user-unit=`.

   This parameter can be specified multiple times.

   .. versionadded:: 195

.. systemd:option:: --user-unit=

   Show messages for the specified user session unit. This will add a match for messages from the unit ("_SYSTEMD_USER_UNIT=" and "_UID=") and additional matches for messages from session systemd and messages about coredumps for the specified unit. A match is also added for "_SYSTEMD_USER_SLICE=:emphasis:`UNIT`", such that if the provided :emphasis:`UNIT` is a :ref:`systemd.slice(5)` unit, all logs of children of the unit will be shown.

   This parameter can be specified multiple times.

   .. versionadded:: 198

.. systemd:option:: -I, --invocation=ID[±offset]|offset

   Show messages from a specific invocation of unit. This will add a match for "_SYSTEMD_INVOCATION_ID=", "OBJECT_SYSTEMD_INVOCATION_ID=", "INVOCATION_ID=", "USER_INVOCATION_ID=".

   A positive :emphasis:`offset` will look up the invocations of a systemd unit from the beginning of the journal, and zero or a negative offset will look up invocations starting from the end of the journal. Thus, :code:`1` means the first invocation found in the journal in chronological order, :code:`2` the second and so on; while :code:`0` is the latest invocation, :code:`-1` the invocation before the latest, and so on.

   If the 32-character :emphasis:`ID` is specified, it may optionally be followed by :emphasis:`±offset` which identifies the invocation relative to the one given by invocation :emphasis:`ID`. Negative values mean earlier invocations and positive values mean later invocations. If :emphasis:`±offset` is not specified, a value of zero is assumed, and the logs for the invocation given by :emphasis:`ID` will be shown.

   :code:`-I` is equivalent to :code:`--invocation=0`, and logs for the latest invocation will be shown.

   When an offset is specified, a unit name must be specified with :code:`-u/--unit=` or :code:`--user-unit=` option.

   When specified with :code:`-b/--boot=`, then invocations are searched within the specified boot.

   .. versionadded:: 257

.. systemd:option:: -t, --identifier=SYSLOG_IDENTIFIER

   Show messages for the specified syslog identifier :emphasis:`SYSLOG_IDENTIFIER`.

   This parameter can be specified multiple times.

   .. versionadded:: 217

.. systemd:option:: -T, --exclude-identifier=SYSLOG_IDENTIFIER

   Exclude messages for the specified syslog identifier :emphasis:`SYSLOG_IDENTIFIER`.

   This parameter can be specified multiple times.

   .. versionadded:: 256

.. systemd:option:: -p, --priority=

   Filter output by message priorities or priority ranges. Takes either a single numeric or textual log level (i.e. between 0/"emerg" and 7/"debug"), or a range of numeric/text log levels in the form FROM..TO. The log levels are the usual syslog log levels as documented in :man-pages:`syslog(3)`, i.e. "emerg" (0), "alert" (1), "crit" (2), "err" (3), "warning" (4), "notice" (5), "info" (6), "debug" (7). If a single log level is specified, all messages with this log level or a lower (hence more important) log level are shown. If a range is specified, all messages within the range are shown, including both the start and the end value of the range. This will add "PRIORITY=" matches for the specified priorities.

   .. versionadded:: 188

.. systemd:option:: --facility=

   Filter output by syslog facility. Takes a comma-separated list of numbers or facility names. The names are the usual syslog facilities as documented in :man-pages:`syslog(3)`. :code:`--facility=help` may be used to display a list of known facility names and exit.

   .. versionadded:: 245

.. systemd:option:: -g, --grep=

   Filter output to entries where the :code:`MESSAGE=` field matches the specified regular expression. PERL-compatible regular expressions are used, see `pcre2pattern(3) <http://pcre.org/current/doc/html/pcre2pattern.html>`_ for a detailed description of the syntax.

   If the pattern is all lowercase, matching is case insensitive. Otherwise, matching is case sensitive. This can be overridden with the :code:`--case-sensitive` option, see below.

   When used with :code:`--lines=` (not prefixed with "+"), :code:`--reverse` is implied.

   .. versionadded:: 237

.. systemd:option:: --case-sensitive[=BOOLEAN]

   Make pattern matching case sensitive or case insensitive.

   .. versionadded:: 237

.. systemd:option:: -k, --dmesg

   Show only kernel messages. This adds the match "_TRANSPORT=kernel". This implies :code:`--boot=0` unless explicitly specified otherwise.

   .. versionadded:: 205

Output Options
##############

The following options control how journal records are printed:

.. systemd:option:: -o, --output=

   Controls the formatting of the journal entries that are shown. Takes one of the following options:

   .. systemd:option:: short

         is the default and generates an output that is mostly identical to the formatting of classic syslog files, showing one line per journal entry.

         .. versionadded:: 206

   .. systemd:option:: short-full

         is very similar, but shows timestamps in the format the :code:`--since=` and :code:`--until=` options accept. Unlike the timestamp information shown in :code:`short` output mode this mode includes weekday, year and timezone information in the output, and is locale-independent.

         .. versionadded:: 232

   .. systemd:option:: short-iso

         is very similar, but shows timestamps in the `RFC 3339 <https://tools.ietf.org/html/rfc3339>`_ profile of ISO 8601.

         .. versionadded:: 206

   .. systemd:option:: short-iso-precise

         as for :code:`short-iso` but includes full microsecond precision.

         .. versionadded:: 234

   .. systemd:option:: short-precise

         is very similar, but shows classic syslog timestamps with full microsecond precision.

         .. versionadded:: 207

   .. systemd:option:: short-monotonic

         is very similar, but shows monotonic timestamps instead of wallclock timestamps.

         .. versionadded:: 206

   .. systemd:option:: short-delta

         as for :code:`short-monotonic` but includes the time difference to the previous entry. Maybe unreliable time differences are marked by a "*".

         .. versionadded:: 252

   .. systemd:option:: short-unix

         is very similar, but shows seconds passed since January 1st 1970 UTC instead of wallclock timestamps ("UNIX time"). The time is shown with microsecond accuracy.

         .. versionadded:: 230

   .. systemd:option:: verbose

         shows the full-structured entry items with all fields.

         .. versionadded:: 206

   .. systemd:option:: export

         serializes the journal into a binary (but mostly text-based) stream suitable for backups and network transfer (see `Journal Export Format <https://systemd.io/JOURNAL_EXPORT_FORMATS#journal-export-format>`_ for more information). To import the binary stream back into native journald format use :ref:`systemd-journal-remote(8)`.

         .. versionadded:: 206

   .. systemd:option:: json

         formats entries as JSON objects, separated by newline characters (see `Journal JSON Format <https://systemd.io/JOURNAL_EXPORT_FORMATS#journal-json-format>`_ for more information). Field values are generally encoded as JSON strings, with three exceptions: 1.          Fields larger than 4096 bytes are encoded as :code:`null` values. (This may be turned off by passing :code:`--all`, but be aware that this may allocate overly long JSON objects.)

         2.          Journal entries permit non-unique fields within the same log entry. JSON does not allow non-unique fields within objects. Due to this, if a non-unique field is encountered a JSON array is used as field value, listing all field values as elements.

         3.          Fields containing non-printable or non-UTF8 bytes are encoded as arrays containing the raw bytes individually formatted as unsigned numbers.

         Note that this encoding is reversible (with the exception of the size limit).

         .. versionadded:: 206

   .. systemd:option:: json-pretty

         formats entries as JSON data structures, but formats them in multiple lines in order to make them more readable by humans.

         .. versionadded:: 206

   .. systemd:option:: json-sse

         formats entries as JSON data structures, but wraps them in a format suitable for `Server-Sent Events <https://developer.mozilla.org/en-US/docs/Server-sent_events/Using_server-sent_events>`_.

         .. versionadded:: 206

   .. systemd:option:: json-seq

         formats entries as JSON data structures, but prefixes them with an ASCII Record Separator character (0x1E) and suffixes them with an ASCII Line Feed character (0x0A), in accordance with `JavaScript Object Notation (JSON) Text Sequences <https://tools.ietf.org/html/rfc7464>`_ ("application/json-seq").

         .. versionadded:: 240

   .. systemd:option:: cat

         generates a very terse output, only showing the actual message of each journal entry with no metadata, not even a timestamp. If combined with the :code:`--output-fields=` option will output the listed fields for each log record, instead of the message.

         .. versionadded:: 206

   .. systemd:option:: with-unit

         similar to :code:`short-full`, but prefixes the unit and user unit names instead of the traditional syslog identifier. Useful when using templated instances, as it will include the arguments in the unit names.

         .. versionadded:: 239

.. systemd:option:: --truncate-newline

   Truncate each log message at the first newline character on output, so that only the first line of each message is displayed.

   .. versionadded:: 254

.. systemd:option:: --output-fields=

   A comma separated list of the fields which should be included in the output. This has an effect only for the output modes which would normally show all fields (:code:`verbose`, :code:`export`, :code:`json`, :code:`json-pretty`, :code:`json-sse` and :code:`json-seq`), as well as on :code:`cat`. For the former, the "__CURSOR", "__REALTIME_TIMESTAMP", "__MONOTONIC_TIMESTAMP", and "_BOOT_ID" fields are always printed.

   .. versionadded:: 236

.. systemd:option:: -n, --lines=

   Show the most recent journal events and limit the number of events shown. The argument is a positive integer or "all" to disable the limit. Additionally, if the number is prefixed with "+", the oldest journal events are used instead. The default value is 10 if no argument is given.

   If :code:`--follow` is used, this option is implied. When not prefixed with "+" and used with :code:`--grep=`, :code:`--reverse` is implied.

.. systemd:option:: -r, --reverse

   Reverse output so that the newest entries are displayed first.

   .. versionadded:: 198

.. systemd:option:: --show-cursor

   The cursor is shown after the last entry after two dashes:

     .. code-block:: sh

        -- cursor: s=0639…

   The format of the cursor is private and subject to change.

   .. versionadded:: 209

.. systemd:option:: --utc

   Express time in Coordinated Universal Time (UTC).

   .. versionadded:: 217

.. systemd:option:: -x, --catalog

   Augment log lines with explanation texts from the message catalog. This will add explanatory help texts to log messages in the output where this is available. These short help texts will explain the context of an error or log event, possible solutions, as well as pointers to support forums, developer documentation, and any other relevant manuals. Note that help texts are not available for all messages, but only for selected ones. For more information on the message catalog, see `Journal Message Catalogs <https://systemd.io/CATALOG>`_.

   Note: when attaching :code:`journalctl` output to bug reports, please do *not* use use :code:`-x`.

   .. versionadded:: 196

.. systemd:option:: -W, --no-hostname

   Do not show the hostname field of log messages. This switch has an effect only on the :code:`short` family of output modes (see above).

   Note: this option does not remove occurrences of the hostname from log entries themselves, so it does not prevent the hostname from being visible in the logs.

   .. versionadded:: 230

.. systemd:option:: --no-full, --full, -l

   Ellipsize fields when they do not fit in available columns. The default is to show full fields, allowing them to wrap or be truncated by the pager, if one is used.

   The old options :code:`-l`/:code:`--full` are not useful anymore, except to undo :code:`--no-full`.

   .. versionadded:: 196

.. systemd:option:: -a, --all

   Show all fields in full, even if they include unprintable characters or are very long. By default, fields with unprintable characters are abbreviated as "blob data". (Note that the pager may escape unprintable characters again.)

.. systemd:option:: -f, --follow

   Show only the most recent journal entries, and continuously print new entries as they are appended to the journal, until Ctrl-C is hit (or the tool is otherwise terminated).

   :code:`journalctl` will send an :ref:`sd_notify(3)` "READY=1" message once it initialized and successfully established its watch on the journal.

.. systemd:option:: --no-tail

   Show all stored output lines, even in follow mode. Undoes the effect of :code:`--lines=`.

.. systemd:option:: -q, --quiet

   Suppresses all informational messages (i.e. "-- Journal begins at …", "-- Reboot --"), any warning messages regarding inaccessible system journals when run as a normal user.

.. systemd:option:: --synchronize-on-exit=

   Takes a boolean argument. If true and operating in :code:`--follow` mode, a journal synchronization request (equivalent to :code:`journalctl --sync`) is issued when :code:`SIGTERM`/:code:`SIGINT` is received, and log output continues until this request completes. This is useful for synchronizing journal log output to the runtime of services or external events, ensuring that any log data enqueued to the logging subsystem by the time :code:`SIGTERM`/:code:`SIGINT` is issued is guaranteed to be processed and displayed by the time log output ends. Defaults to false.

   .. versionadded:: 258

Pager Control Options
#####################

The following options control page support:

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove no-pager|
    :end-before: .. inclusion-end-marker-do-not-remove no-pager|

.. systemd:option:: -e, --pager-end

   Immediately jump to the end of the journal inside the implied pager tool. This implies :code:`--lines=1000` and :code:`--boot=0` unless explicitly specified otherwise, to guarantee that the pager will not buffer logs of unbounded size. Note that this option is only supported for the :man-pages:`less(1)` pager.

   .. versionadded:: 198

Forward Secure Sealing (FSS) Options
####################################

The following options may be used together with the :code:`--setup-keys` command described below:

.. systemd:option:: --interval=

   Specifies the change interval for the sealing key when generating an FSS key pair with :code:`--setup-keys`. Shorter intervals increase CPU consumption but shorten the time range of undetectable journal alterations. Defaults to 15min.

   Note, :code:`--output=json-sse` and :code:`--output=json-seq` are silently migrated to :code:`--output=json`.

   .. versionadded:: 189

.. systemd:option:: --verify-key=

   Specifies the FSS verification key to use for the :code:`--verify` operation.

   .. versionadded:: 189

.. systemd:option:: --force

   When :code:`--setup-keys` is passed and Forward Secure Sealing (FSS) has already been configured, recreate FSS keys.

   .. versionadded:: 206

Commands
########

The following commands are understood. If none is specified the default is to display journal records:

.. systemd:option:: -N, --fields

   Print all field names currently used in all entries of the journal.

   .. versionadded:: 229

.. systemd:option:: -F, --field=

   Print all possible data values the specified field can take in all entries of the journal.

   .. versionadded:: 195

.. systemd:option:: --list-boots

   Show a tabular list of boot numbers (relative to the current boot), their IDs, and the timestamps of the first and last message pertaining to the boot. When specified with :code:`-n/--lines=[+]:emphasis:`N`` option, only the first (when the number prefixed with "+") or the last (without prefix) :emphasis:`N` entries will be shown. When specified with :code:`-r/--reverse`, the list will be shown in the reverse order.

   .. versionadded:: 209

.. systemd:option:: --list-invocations

   List invocation IDs of a unit. Requires a unit name with :code:`-u/--unit=` or :code:`--user-unit=`. Show a tabular list of invocation numbers (relative to the current or latest invocation), their IDs, and the timestamps of the first and last message pertaining to the invocation. When :code:`-b/-boot` is specified, invocations in the boot will be shown. When specified with :code:`-n/--lines=[+]:emphasis:`N`` option, only the first (when the number prefixed with "+") or the last (without prefix) :emphasis:`N` entries will be shown. When specified with :code:`-r/--reverse`, the list will be shown in the reverse order.

   .. versionadded:: 257

.. systemd:option:: --disk-usage

   Shows the current disk usage of all journal files. This shows the sum of the disk usage of all archived and active journal files.

   .. versionadded:: 190

.. systemd:option:: --vacuum-size=, --vacuum-time=, --vacuum-files=

   :code:`--vacuum-size=` removes the oldest archived journal files until the disk space they use falls below the specified size. Accepts the usual "K", "M", "G" and "T" suffixes (to the base of 1024).

   :code:`--vacuum-time=` removes archived journal files older than the specified timespan. Accepts the usual "s" (default), "m", "h", "days", "weeks", "months", and "years" suffixes, see :ref:`systemd.time(7)` for details.

   :code:`--vacuum-files=` leaves only the specified number of separate journal files.

   Note that running :code:`--vacuum-size=` has only an indirect effect on the output shown by :code:`--disk-usage`, as the latter includes active journal files, while the vacuuming operation only operates on archived journal files. Similarly, :code:`--vacuum-files=` might not actually reduce the number of journal files to below the specified number, as it will not remove active journal files.

   :code:`--vacuum-size=`, :code:`--vacuum-time=` and :code:`--vacuum-files=` may be combined in a single invocation to enforce any combination of a size, a time and a number of files limit on the archived journal files. Specifying any of these three parameters as zero is equivalent to not enforcing the specific limit, and is thus redundant.

   These three switches may also be combined with :code:`--rotate` into one command. If so, all active files are rotated first, and the requested vacuuming operation is executed right after. The rotation has the effect that all currently active files are archived (and potentially new, empty journal files opened as replacement), and hence the vacuuming operation has the greatest effect as it can take all log data written so far into account.

   .. versionadded:: 218

.. systemd:option:: --verify

   Check the journal file for internal consistency. If the file has been generated with FSS enabled and the FSS verification key has been specified with :code:`--verify-key=`, authenticity of the journal file is verified.

   .. versionadded:: 189

.. systemd:option:: --sync

   Asks the journal daemon to write all yet unwritten journal data to the backing file system and synchronize all journals. This call does not return until the synchronization operation is complete. This command guarantees that any log messages written before its invocation are safely stored on disk at the time it returns.

   .. versionadded:: 228

.. systemd:option:: --relinquish-var

   Asks the journal daemon for the reverse operation to :code:`--flush`: if requested the daemon will write further log data to :code:`/run/log/journal/` and stops writing to :code:`/var/log/journal/`. A subsequent call to :code:`--flush` causes the log output to switch back to :code:`/var/log/journal/`, see above.

   .. versionadded:: 243

.. systemd:option:: --smart-relinquish-var

   Similar to :code:`--relinquish-var`, but executes no operation if the root file system and :code:`/var/log/journal/` reside on the same mount point. This operation is used during system shutdown in order to make the journal daemon stop writing data to :code:`/var/log/journal/` in case that directory is located on a mount point that needs to be unmounted.

   .. versionadded:: 243

.. systemd:option:: --flush

   Asks the journal daemon to flush any log data stored in :code:`/run/log/journal/` into :code:`/var/log/journal/`, if persistent storage is enabled. This call does not return until the operation is complete. Note that this call is idempotent: the data is only flushed from :code:`/run/log/journal/` into :code:`/var/log/journal/` once during system runtime (but see :code:`--relinquish-var` below), and this command exits cleanly without executing any operation if this has already happened. This command effectively guarantees that all data is flushed to :code:`/var/log/journal/` at the time it returns.

   .. versionadded:: 217

.. systemd:option:: --rotate

   Asks the journal daemon to rotate journal files. This call does not return until the rotation operation is complete. Journal file rotation has the effect that all currently active journal files are marked as archived and renamed, so that they are never written to in future. New (empty) journal files are then created in their place. This operation may be combined with :code:`--vacuum-size=`, :code:`--vacuum-time=` and :code:`--vacuum-file=` into a single command, see above.

   .. versionadded:: 227

.. systemd:option:: --header

   Instead of showing journal contents, show internal header information of the journal fields accessed.

   This option is particularly useful when trying to identify out-of-order journal entries, as happens for example when the machine is booted with the wrong system time.

   .. versionadded:: 187

.. systemd:option:: --list-catalog [128-bit-ID…]

   List the contents of the message catalog as a table of message IDs, plus their short description strings.

   If any :emphasis:`128-bit-ID`\s are specified, only those entries are shown.

   .. versionadded:: 196

.. systemd:option:: --dump-catalog [128-bit-ID…]

   Show the contents of the message catalog, with entries separated by a line consisting of two dashes and the ID (the format is the same as :code:`.catalog` files).

   If any :emphasis:`128-bit-ID`\s are specified, only those entries are shown.

   .. versionadded:: 199

.. systemd:option:: --update-catalog

   Update the message catalog index. This command needs to be executed each time new catalog files are installed, removed, or updated to rebuild the binary catalog index.

   .. versionadded:: 196

.. systemd:option:: --setup-keys

   Instead of showing journal contents, generate a new key pair for Forward Secure Sealing (FSS). This will generate a sealing key and a verification key. The sealing key is stored in the journal data directory and shall remain on the host. The verification key should be stored externally. Refer to the :code:`Seal=` option in :man-pages:`journald.conf(5)` for information on Forward Secure Sealing and for a link to a refereed scholarly paper detailing the cryptographic theory it is based on.

   .. versionadded:: 189

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove help|
    :end-before: .. inclusion-end-marker-do-not-remove help|

.. include:: standard-options.rst
    :start-after: .. inclusion-marker-do-not-remove version|
    :end-before: .. inclusion-end-marker-do-not-remove version|

Exit status
###########

On success, 0 is returned; otherwise, a non-zero failure code is returned.

.. include:: common-variables.rst

Examples
########

Without arguments, all collected logs are shown unfiltered:

.. code-block:: sh

   journalctl

With one match specified, all entries with a field matching the expression are shown:

.. code-block:: sh

   journalctl _SYSTEMD_UNIT=avahi-daemon.service
   journalctl _SYSTEMD_CGROUP=/user.slice/user-42.slice/session-c1.scope

If two different fields are matched, only entries matching both expressions at the same time are shown:

.. code-block:: sh

   journalctl _SYSTEMD_UNIT=avahi-daemon.service _PID=28097

If two matches refer to the same field, all entries matching either expression are shown:

.. code-block:: sh

   journalctl _SYSTEMD_UNIT=avahi-daemon.service _SYSTEMD_UNIT=dbus.service

If the separator "+" is used, two expressions may be combined in a logical OR. The following will show all messages from the Avahi service process with the PID 28097 plus all messages from the D-Bus service (from any of its processes):

.. code-block:: sh

   journalctl _SYSTEMD_UNIT=avahi-daemon.service _PID=28097 + _SYSTEMD_UNIT=dbus.service

To show all fields emitted *by* a unit and a unit and *about* the unit, option the unit, option :code:`-u`/:code:`--unit=` should be used. :code:`journalctl -u name` expands to a complex filter similar to

.. code-block:: sh

   _SYSTEMD_UNIT=name.service
     + UNIT=name.service _PID=1
     + OBJECT_SYSTEMD_UNIT=name.service _UID=0
     + COREDUMP_UNIT=name.service _UID=0 MESSAGE_ID=fc2e22bc6ee647b6b90729ab34a250b1

(see :ref:`systemd.journal-fields(7)` for an explanation of those patterns).

Show all logs generated by the D-Bus executable:

.. code-block:: sh

   journalctl /usr/bin/dbus-daemon

Show all kernel logs from previous boot:

.. code-block:: sh

   journalctl -k -b -1

Show a live log display from a system service :code:`apache.service`:

.. code-block:: sh

   journalctl -f -u apache

See Also
########

:ref:`systemd(1)`, :ref:`systemd-cat(1)`, :ref:`systemd-journald.service(8)`, :man-pages:`systemctl(1)`, :man-pages:`coredumpctl(1)`, :ref:`systemd.journal-fields(7)`, :man-pages:`journald.conf(5)`, :ref:`systemd.time(7)`, :ref:`systemd-journal-remote.service(8)`, :ref:`systemd-journal-upload.service(8)`


